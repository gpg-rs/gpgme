[package]
name = "gpgme"
description = "GPGme bindings for Rust"
repository = "https://github.com/johnschug/rust-gpgme"
version = "0.1.0"
authors = ["John Schug <john.ips.schug@gmail.com>"]
license = "LGPL-2.1"
keywords = ["gpg", "gpgme", "cryptography"]

[dependencies]
libc = "0.1"
bitflags = "0.1"
enum_primitive = "*"

[dependencies.gpgme-sys]
path = "gpgme-sys"
version = "0.1.0"

[dev-dependencies]
getopts = "0.2"
